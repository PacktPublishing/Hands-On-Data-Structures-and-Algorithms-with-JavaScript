import { ProfileComponent } from './profile.component';

export const ProfileRoutes = [
    { path: 'profile', component: ProfileComponent, data: { keymap: 'ctrl+p'} },
];

export const ProfileComponents = [
    ProfileComponent
];